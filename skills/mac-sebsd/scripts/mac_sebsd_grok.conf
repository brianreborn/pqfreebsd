# Light-ware License. Copyright (c) 2026 Brian Fundakowski Feldman. See LICENSE in the distribution.
# /usr/local/etc/mac_sebsd_grok.conf — sourced via rc.conf(5) / rc.subr(8)
# Copy into /etc/rc.conf or keep here and:
#   . /usr/local/etc/mac_sebsd_grok.conf
#
# mac_sebsd_grok_enable="NO"   # YES: boot kldload only, enabled=0
# mac_sebsd_grok_dir="/usr/local/etc/mac_sebsd_grok"
# mac_sebsd_grok_db="/var/db/mac_sebsd_grok"
# mac_sebsd_grok_contexts="/etc/lomac.contexts"
# mac_sebsd_grok_checkpoint="NO"   # YES: zpool checkpoint (one per pool; zpool-checkpoint(8))
